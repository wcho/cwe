{
  "name": "cwe-eslint-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": ["plugin:jest/recommended"],
    "plugins": ["jest"],
    "rules": {
      "모르는룰": "error"
    }
  }
}
